 R←Test_0121(stopFlag batchFlag);myIni;filename;keys;io;ml;buffer;buf;NS;⎕TRAP
⍝ Take an INI file that contains invalid value names.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 filename←'TestCases/InvalidValueNameClassic.ini'

 myIni←⎕NEW ##.IniFiles(,⊂filename)
 :Trap 0
     NS←myIni.Convert ⎕NS''
     →T.FailsIf
 :Else
     →T.FailsIf'Invalid value name(s):'{⍺≢(⍴⍺)↑⍵}0⊃⎕DM
 :EndTrap

 R←T._OK
