 R←Test_0126(stopFlag batchFlag);myIni;filename;⎕TRAP
⍝ Check an invalid old-style INI file: invalid value name.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed
 filename←'TestCases/oldstyle_invalid_2'

 :Trap 0
     myIni←⎕NEW ##.IniFiles(,⊂filename)
     →T.FailsIf
 :Else
     →T.FailsIf'Invalid value name(s): App Name'≢0⊃⎕DM
 :EndTrap

 R←T._OK
