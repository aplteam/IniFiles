 R←Test_0120(stopFlag batchFlag);myIni;filename;keys;io;ml;buffer;buf;⎕TRAP
⍝ Take an INI file that contains invalid value names.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 filename←'TestCases/InvalidValueName.ini'

 :Trap 0
     myIni←⎕NEW ##.IniFiles(,⊂filename)
     →FailsIf 1
 :Else
     →FailsIf'Invalid value name(s):'{⍺≢(⍴⍺)↑⍵}0⊃⎕DM
 :EndTrap

 R←∆OK
