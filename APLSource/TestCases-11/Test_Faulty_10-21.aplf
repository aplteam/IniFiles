 R←Test_Faulty_10(stopFlag batchFlag);myIni;filename;keys;io;ml;buffer;buf;⎕TRAP
⍝ Process an inconsistent (old-style/new-style) INI file.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 filename←'TestCases/AlmostOldstyle.ini'

 :Trap 0
     myIni←⎕NEW ##.IniFiles(,⊂filename)
     →FailsIf 1
 :Else
     →FailsIf('TestCases/AlmostOldstyle.ini is inconsistent - neither old-style nor new-style'){⍺≢(⍴⍺)↑⍵}0⊃⎕DM
 :EndTrap

 R←∆OK
