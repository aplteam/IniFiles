 R←Test_0129(stopFlag batchFlag);path;buffer;⎕TRAP
⍝ Once two comment lines (but not one!) caused a bug.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 path←'TestCases/'

 buffer←⎕NEW ##.IniFiles((path,'curlies.ini')1)
 →FailsIf 5 3≢⍴buffer.Get ⍬ ⍬
 →FailsIf 37≠⍴⊃buffer.Get'Alpha' 'AlphaKeys'

 R←∆OK
