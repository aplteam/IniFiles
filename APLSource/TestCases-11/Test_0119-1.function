 R←Test_0119(stopFlag batchFlag);myIni;filename;keys;io;ml;buffer;buf;⎕TRAP
⍝ Take an INI file that contains invalid section names.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 filename←'TestCases/InvalidSectionName.ini'

 :Trap 0
     myIni←⎕NEW ##.IniFiles(,⊂filename)
     →FailsIf 1
 :Else
     →FailsIf'Invalid section name(s):'{⍺≢(⍴⍺)↑⍵}0⊃⎕DM
 :EndTrap

 R←∆OK
