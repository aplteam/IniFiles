 R←Test_0125(stopFlag batchFlag);myIni;filename;⎕TRAP
⍝ Check an invalid old-style INI file: invalid section name.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed
 filename←'TestCases/oldstyle_invalid_1'

 :Trap 0
     myIni←⎕NEW ##.IniFiles(,⊂filename)
     →FailsIf
 :Else
     →FailsIf'Invalid section name(s): [GEN ERAL]'≢0⊃⎕DM
 :EndTrap

 R←∆OK
